[master]
# IP Publico do Master

[workers]
# IPs Publicos dos Workers

[kubernetes:children]
master
workers

[kubernetes:vars]
ansible_python_interpreter=/usr/bin/python3
ansible_user=user # Usuario da máquina
ansible_ssh_private_key_file=<path> # Caminho da chave privada